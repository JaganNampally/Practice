name: Simple Test Action

on:
  push:
    branches: [ "master" ]
  schedule:
    - cron: "* * * * *"    # ⏰ Trigger every minute (UTC)
  workflow_dispatch:       # 🖱 Optional: Manual trigger from UI
  workflow_run:
    workflows: ["Python script job"]
    types: 
      - completed

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Say Hello
        run: echo "👋 Hello from GitHub Actions!"

      - name: Run dummy script
        run: |
          echo "This is a dummy step and secret is ${{secrets.REPO_OWNER}}"
          echo "You can replace this with your actual logic"


name: Simple Test Action

on:
  push:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Say Hello
        run: echo "👋 Hello from GitHub Actions!"

      - name: Run dummy script
        run: |
          echo "This is a dummy step"
          echo "You can replace this with your actual logic"

